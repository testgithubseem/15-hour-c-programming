/*write a program to impliment recursive function*/
/*the sum of digits of a number recursion*/
#include<stdio.h>
int sum(int n) 
 int main()
 {
int n,result;
printf("enter the number:");
scanf("%d",&n);
result=sum(n);
printf("sum of digit %d\n",n,result);
return 0;
}
int sum(int n)
{
if(num!=0);
{
return(n%10+sum(n\10));
}
else
{
return0;
}
